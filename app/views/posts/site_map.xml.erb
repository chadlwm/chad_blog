<?xml version="1.0" encoding="UTF-8"?>
<urlset>
  <% @posts.each do |post| %>
    <url>
      <loc><%= post_url slug: post.slug %></loc>
      <lastmod><%= post.created_at.strftime("%F") %></lastmod>
      <changefreq>always</changefreq>
      <priority>1.0</priority>
    </url>
  <% end %>
</urlset>
